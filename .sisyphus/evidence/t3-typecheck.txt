T3: Effect Service Definitions — Typecheck Evidence
=====================================================

Task: Create typescript/src/engine/agent.ts and typescript/src/services.ts using Effect v4 beta APIs

Files Created:
- typescript/src/engine/agent.ts (AgentEngine service)
- typescript/src/services.ts (5 service tags)

Typecheck Command:
  cd /Users/phall/workspace/symphony/typescript && npx tsc --noEmit

Result: SUCCESS
Exit Code: 0
Output: (no errors)

Service Definitions Implemented:
1. AgentEngine (typescript/src/engine/agent.ts)
   - Uses ServiceMap.Service<AgentEngine, Shape>()("AgentEngine")
   - Defines createSession method returning Effect<AgentSession, AgentEngineError>
   - Includes AgentEngineError and AgentSessionError interfaces
   - Includes AgentSession interface with runTurn, abort, dispose methods

2. WorkflowStore (typescript/src/services.ts)
   - get(): Effect<WorkflowDefinition, WorkflowError>
   - getResolved(): Effect<ResolvedConfig, ConfigError>

3. TrackerClient (typescript/src/services.ts)
   - fetchCandidateIssues(): Effect<ReadonlyArray<Issue>, TrackerError>
   - fetchIssueStatesByIds(ids): Effect<ReadonlyArray<Issue>, TrackerError>
   - fetchIssuesByStates(states): Effect<ReadonlyArray<Issue>, TrackerError>

4. WorkspaceManager (typescript/src/services.ts)
   - createForIssue(identifier): Effect<Workspace, WorkspaceError>
   - removeForIssue(identifier): Effect<void, WorkspaceError>
   - runHook(hook, workspacePath): Effect<void, never>

5. PromptEngine (typescript/src/services.ts)
   - render(template, issue, attempt): Effect<string, PromptError>

6. OrchestratorStateRef (typescript/src/services.ts)
   - ref: Ref<OrchestratorState>

API Verification:
- Confirmed ServiceMap.Service exists in effect@4.0.0-beta.27
- Verified class extension pattern: class X extends ServiceMap.Service<X, Shape>()("identifier") {}
- All imports use .js extension for NodeNext module resolution
- All types imported from ../types.js
- Ref imported from effect (unchanged in v4)

Compliance:
✓ Uses Effect v4 beta APIs (ServiceMap.Service)
✓ No v3 API usage (Context.Tag, Effect.Tag, Effect.Service)
✓ No implementation logic (service definitions only)
✓ All types use readonly pattern
✓ Typecheck passes with exit code 0
